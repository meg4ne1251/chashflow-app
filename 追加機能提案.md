# 家計簿アプリ 追加機能提案

## 1. 貯蓄目標（Savings Goals）

### 概要
旅行資金、緊急資金、大きな買い物など「目標額に向けた積立進捗」を可視化する機能。

### 背景・動機
既存の予算管理は「使いすぎ防止」が目的だが、貯蓄目標は「貯める動機付け」として補完的に働く。目標に向かって進捗が見えることで、節約のモチベーションが維持しやすくなる。

### 機能詳細
- 目標の作成（名前、目標額、期限、対象アカウント、アイコン/カラー）
- 進捗バーによる達成率の可視化
- ダッシュボードへの目標サマリー表示
- 目標達成時の通知
- 複数目標の同時管理（優先度付き）
- 月ごとの積立推奨額の自動計算（残額 ÷ 残月数）

### 想定データモデル
- `savings_goals` テーブル: id, name, target_amount, current_amount, deadline, account_id, icon, color, is_achieved, created_at, updated_at

### 優先度: ★★★★★

---

## 2. クレジットカード締め日・引落日管理

### 概要
クレジットカードの「今月の利用額」「来月の引落予定額」を自動集計し、引落に備えられる機能。

### 背景・動機
日本のクレジットカードは締め日と引落日が1〜2ヶ月ずれるため、「今月使った額」と「来月口座から引かれる額」が一致しない。これを自動計算し可視化することで、口座残高不足による引落失敗を防げる。

### 機能詳細
- アカウント（クレジットカードタイプ）に締め日・引落日・引落先口座を設定
- 締め日に基づく「今月の利用額」の自動集計
- 「来月の引落予定額」「再来月の引落予定額」の計算・表示
- ダッシュボードに引落予定カレンダーまたはサマリーを表示
- 引落日の数日前にリマインド通知
- 引落先口座の残高と引落予定額の比較（残高不足警告）

### 想定データモデル
- `accounts` テーブルに追加: closing_day (締め日, 1-31), payment_day (引落日, 1-31), payment_account_id (引落先口座FK)

### 優先度: ★★★★★

---

## 3. 割り勘・立替管理（Split / Reimbursement Tracking）

### 概要
「友人の分を立て替えた」「夫婦で折半」などの未精算金額を追跡し、実質支出を正確に把握する機能。

### 背景・動機
飲み会の幹事や旅行の立替など、一時的に他人の分を支払うケースは多い。これを通常の支出と混同すると、実質の家計支出が正確に把握できなくなる。立替→回収のフローを管理することで、本当の支出だけを分析に反映できる。

### 機能詳細
- 取引に「立替」フラグと立替相手（名前）を設定
- 立替相手ごとの未精算残高一覧
- 精算（回収/支払）の記録と自動消し込み
- 未精算一覧の表示（誰にいくら貸している/借りている）
- 分析時に立替分を除外するフィルタオプション
- 割り勘計算機能（合計額 ÷ 人数、端数処理）

### 想定データモデル
- `split_persons` テーブル: id, name, created_at
- `split_records` テーブル: id, transaction_id, person_id, amount, is_settled, settled_at, settled_transaction_id, memo

### 優先度: ★★★☆☆

---

## 4. サブスクリプション管理ビュー

### 概要
定期取引の中から月額/年額のサブスクリプションサービスを抽出し、専用の一覧・合計額で表示する機能。

### 背景・動機
Netflix、Spotify、クラウドストレージなど、気づかないうちにサブスクが増えて月々の固定費が膨らみがち。一覧で「月額サブスク合計」が見えるだけで、不要なサービスの見直しにつながる。

### 機能詳細
- 定期取引に「サブスクリプション」フラグを追加
- サブスク専用の一覧ページ
  - サービス名、月額/年額、カテゴリ、次回請求日
  - 月額換算での合計表示（年額サービスは÷12で月額換算）
- 年間サブスク総額の表示
- 契約日・更新日の記録
- 解約メモ欄（解約手順URLなど）
- 利用頻度が低いサブスクの「見直し提案」（オプション）

### 想定データモデル
- `recurring_transactions` テーブルに追加: is_subscription (boolean), service_url, cancel_note

### 優先度: ★★★☆☆

---

## 5. カスタムレポート期間

### 概要
月次・年次に限らず、任意の期間を指定して分析・レポートを生成できる機能。

### 背景・動機
「旅行中の3日間でいくら使ったか」「ボーナス月の前後を比較したい」「年度（4月〜3月）で集計したい」など、固定の月次/年次では対応できない分析ニーズがある。タグと組み合わせることで、イベント単位の支出分析が可能になる。

### 機能詳細
- 分析ページに「カスタム期間」モードを追加
- 開始日・終了日を自由に指定
- 既存の分析機能（カテゴリ別内訳、収支サマリー）をカスタム期間で利用可能
- タグフィルタとの併用（例: 「旅行タグ × 3/10〜3/15」）
- よく使う期間のプリセット保存
- PDF出力にもカスタム期間を反映

### 想定データモデル
- 新規テーブル不要（既存の分析エンドポイントにstart_date, end_dateパラメータを追加）

### 優先度: ★★★☆☆

---

## 6. 資産推移グラフ（Net Worth Tracking）

### 概要
全口座の残高合計を時系列で追跡し、「資産がどう増減しているか」を長期的に可視化する機能。

### 背景・動機
日々の収支だけでなく「全体として資産が増えているのか減っているのか」を俯瞰できることが、家計管理の最終的なゴール。月末時点の総資産をスナップショットとして記録し、数ヶ月〜数年の推移を折れ線グラフで表示する。

### 機能詳細
- 月末（または任意のタイミング）に全口座残高のスナップショットを自動記録
- 総資産の推移を折れ線グラフで表示
- 口座別の残高推移（積み上げ面グラフ）
- 前月比・前年同月比の増減表示
- 負債（クレジットカード残高）を考慮した純資産表示
- ダッシュボードに直近の資産推移ミニグラフを配置

### 想定データモデル
- `net_worth_snapshots` テーブル: id, snapshot_date, total_assets, total_liabilities, net_worth, created_at
- `account_snapshots` テーブル: id, snapshot_id, account_id, balance, created_at

### 優先度: ★★★★☆

---

## 7. 支出の異常検知・インサイト

### 概要
過去の支出パターンと比較して異常や注目すべき変化を自動検出し、通知やダッシュボードで知らせる機能。

### 背景・動機
家計簿をつけていても、データを見返す習慣がないと気づきを得にくい。「先月より食費が30%増」「今月はまだ3日残っているが予算の95%消化」など、システムが自動で気づきを提供することで、能動的に分析しなくても家計の変化に対応できる。

### 機能詳細
- カテゴリ別の前月比・前年同月比の異常検出（閾値: ±20%以上の変動）
- 予算消化ペースの予測（「このペースだと月末に予算を○○円超過します」）
- 連続支出の検出（「3日連続で外食しています」）
- 月の前半/後半の支出バランス分析
- ダッシュボードに「今月のインサイト」カードとして表示
- 通知システムとの連携（重要なインサイトはプッシュ通知）

### 想定実装
- 既存の分析エンドポイントの拡張で実装可能
- ルールベース（閾値比較）から開始し、段階的に高度化
- バッチ処理（日次）でインサイトを生成し、通知テーブルに保存

### 優先度: ★★★★☆

---

## 8. フィルタの保存（Smart Filters / Saved Views）

### 概要
よく使うフィルタ条件を保存して、ワンタップで呼び出せる機能。

### 背景・動機
「食費＋外食タグ＋今月」「交通費＋通勤以外」など、繰り返し使うフィルタ条件を毎回手動で設定するのは手間。保存しておけば、定期的な振り返りや分析が効率化される。

### 機能詳細
- 取引一覧の現在のフィルタ条件を名前付きで保存
- 保存可能な条件: 期間、カテゴリ、アカウント、タグ、キーワード、金額範囲、取引タイプ
- 保存済みフィルタの一覧表示・編集・削除
- フィルタの並び替え（使用頻度順・手動順）
- 取引一覧ページにフィルタ切り替えのドロップダウンまたはタブを追加

### 想定データモデル
- `saved_filters` テーブル: id, name, filter_config (JSONB), sort_order, use_count, last_used_at, created_at, updated_at

### 優先度: ★★☆☆☆

---

## まとめ

| # | 機能 | ユーザー価値 | 実装コスト | 優先度 |
|---|------|------------|-----------|--------|
| 1 | 貯蓄目標 | 高 | 中 | ★★★★★ |
| 2 | クレカ締め日・引落日管理 | 高 | 低〜中 | ★★★★★ |
| 3 | 割り勘・立替管理 | 中 | 中 | ★★★☆☆ |
| 4 | サブスクリプション管理ビュー | 中 | 低 | ★★★☆☆ |
| 5 | カスタムレポート期間 | 中 | 低 | ★★★☆☆ |
| 6 | 資産推移グラフ | 高 | 中 | ★★★★☆ |
| 7 | 支出の異常検知・インサイト | 中〜高 | 低 | ★★★★☆ |
| 8 | フィルタの保存 | 低〜中 | 低 | ★★☆☆☆ |

### 推奨する実装順序
1. **クレカ締め日・引落日管理** — 既存テーブルへのカラム追加で着手しやすい
2. **貯蓄目標** — 新機能だがスキーマがシンプルで独立性が高い
3. **支出の異常検知・インサイト** — 既存の分析基盤を活用でき、低コストで価値が高い
4. **資産推移グラフ** — スナップショットの仕組みを先に作れば段階的に拡張可能
5. **カスタムレポート期間** — 既存エンドポイントのパラメータ拡張が中心
6. **サブスクリプション管理ビュー** — 定期取引への小さな拡張
7. **割り勘・立替管理** — 独立した新機能モジュールとして実装
8. **フィルタの保存** — UIの利便性向上、優先度は低いが実装コストも低い
